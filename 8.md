## Identity in Formal Language Theory

`(a + b)* = (a* b*)*`

- `+` means union (either `a` or `b`)
- `*` means zero or more repetitions (Kleene star)

Both sides represent all possible strings over `{a, b}`.
